#include "Media.h"
#include <iostream>

using namespace std;

class VideoGame:public Media {
 public:
  VideoGame(char* newTitle, int year, char* publisher, float rating);
  void printMedia();
  char* getPublisher();
  float getRating();
  ~VideoGame();
private:
  char* publisher;
  float rating;
};
